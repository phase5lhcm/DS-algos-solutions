const isValidSubsequence = (array, sequence) => {
  let i = 0;
  let j = 0;

  // base case
  if (array.length === 1 && sequence.length === 1) {
    array[i] === sequence[j] ? true : false;
  }

  
};
